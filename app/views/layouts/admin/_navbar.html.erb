<% if admin_user_signed_in? %>
  <header class="bg-white p-4 shadow flex justify-between items-center">
    <h1 class="text-xl font-semibold text-gray-800">Admin Page</h1>
    <div class="flex items-center space-x-4">
      <input type="text" placeholder="Search..." class="px-3 py-1 rounded border border-gray-300">
      <button class="bg-blue-500 text-white px-4 py-1 rounded hover:bg-blue-600">Search</button>
    </div>
  </header>
<% end %>