<!DOCTYPE html>
<html>
  <head>
    <title>Student Voting System</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="flex min-h-screen bg-gray-100 font-sans" data-controller="sweet-alert" data-sweet-alert-controller-path-value="<%= "#{request.base_url}/#{controller_name}" %>">
      <div class="flex-1 flex flex-col">
        <%= render 'layouts/students/navbar' if current_user %>
        <%= render 'layouts/flash' %>

        <main class="flex-1 overflow-y-auto bg-gray-100">
            <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>

